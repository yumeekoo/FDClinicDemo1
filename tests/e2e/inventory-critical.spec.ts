import { test, expect } from '@playwright/test';

test('inventory KHÔNG trừ khi payment pending', async ({ page }) => {});
test('inventory trừ CHỈ SAU payment confirmed', async ({ page }) => {});
test('FEFO: batch expiry sớm trừ trước', async ({ page }) => {});
