import { describe, test, expect } from 'vitest';

describe('PrescriptionForm', () => {
  test('add drug item → xuất hiện trong list', () => {});
  test('remove drug item → biến mất khỏi list', () => {});
  test('quantity = 0 → không cho save', () => {});
  test('tổng số ngày dùng tự tính đúng', () => {});
  test('search tên thuốc → autocomplete', () => {});
  test('submit → prescription record tạo', () => {});
  test('submit với 0 items → error', () => {});
});